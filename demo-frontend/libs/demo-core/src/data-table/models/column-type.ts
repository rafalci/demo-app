/**
 * Supported column types that can be set in datatable config
 */
export enum ColumnType {
  /**
   * Plain text column
   */
  PLAIN_TEXT,
  /**
   * Number column
   */
  NUMBER,
  /**
   * Boolean value column
   */
  BOOLEAN,
  /**
   * Date column
   */
  DATE
}
